[project]
name = "deepresearch-agent"
version = "0.1.0"
description = "Autonomous research agent built with LangGraph"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
